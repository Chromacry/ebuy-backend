version: "3.2"
services:
  projectx-backend:
    build:
      context: .
      dockerfile: Dockerfile
    restart: always
    ports:
      - "8080:8080"
    environment:
      - PORT=8080
    volumes:
      - ./:/projectx-backend
      - ./projectx-backend/node_modules